generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_CONNECTION_STRING")
}

model Fruit {
  id   String @id @default(uuid())
  name String

  @@map("fruits")
}
